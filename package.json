{
    "private": true,
    "type": "module",
    "scripts": {
    "build": "vite build",
    "heroku-postbuild": "npm install && npm run build"
},
    "devDependencies": {
        "axios": "^1.7.4",
        "laravel-vite-plugin": "^1.0",
        "vite": "^5.0"
    }
}
